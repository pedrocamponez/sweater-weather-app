import React from "react";
import './header.css'

const Header = () => {
    return (
        <header>
            <h1>Welcome to the Sweater Weather App :)</h1>
        </header>
    );
};

export default Header;
